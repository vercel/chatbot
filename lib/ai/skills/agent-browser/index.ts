/**
 * Agent-browser skill exports.
 *
 * Structure:
 * - skill.ts: Core workflow (loaded into system prompt)
 * - references/: Detailed docs (loaded on demand by the agent)
 */
export { agentBrowserSkill } from './skill';
