name: Vercel
icon: cloud
accentColor: "#000000"
keyFields:
  - { field: project, type: text }
  - { field: deploymentUrl, type: text }
  - { field: buildDuration, type: text }
expandedLayout:
  - buildLog
  - envVars
functions:
  - deploy
  - list_deployments
  - add_domain
  - health_check
